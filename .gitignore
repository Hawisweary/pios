# Private data — lives in the nested private repo pios-vault, never in the public repo
/vault/

# Derived state — rebuildable from vault/ (Article 9)
pios.db
pios.db-*

.env.local
.DS_Store
__pycache__/
