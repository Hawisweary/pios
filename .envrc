# pios — project environment
source_env "$HOME/Projects/.envrc"   # inherit global secrets

export PORT=8200
export API_PORT=8200
export WEB_PORT=3200
export NEXT_PUBLIC_API_URL="http://localhost:8200"

# Add project-specific vars below:
# export DATABASE_URL=...

dotenv_if_exists .env.local
